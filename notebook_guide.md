# House Prices 预测模型改进版使用指南

## 文件说明

- **House_Prices_Improved.ipynb** - 改进版notebook，包含增强的可视化和特征工程
- **Untitled-2.ipynb** - 原始baseline notebook
- **eda_visuals.py** - 在notebook中自动生成的可视化工具模块

## 主要改进

### 1. 数据探索增强（新增15+可视化）

#### 目标变量分析
- SalePrice统计描述（均值、中位数、偏度、峰度）
- 原始分布 vs Log1p变换分布对比
- QQ图正态性检验

#### 数值特征分析
- Top 8最相关特征与SalePrice散点图
- 关键特征箱线图（GrLivArea, TotalBsmtSF, GarageArea等）
- 相关性热力图（Top 20特征）

#### 类别特征分析
- 基数统计和可视化
- Neighborhood vs SalePrice (Top 15社区)
- OverallQual vs SalePrice
- GarageType vs SalePrice (小提琴图)
- Foundation vs SalePrice

#### 特征交互
- GrLivArea vs SalePrice（按Neighborhood着色）
- YearBuilt vs SalePrice（按OverallQual着色）
- 异常值识别和标注

### 2. 特征工程优化

#### 改进的缺失值处理
- **LotFrontage**: 按Neighborhood分组中位数填充（而非全局均值）
- **车库特征**: 6个车库相关特征统一处理（GarageType, GarageFinish等）
- **地下室特征**: 11个地下室相关特征统一处理（BsmtQual, BsmtCond等）

#### 新增派生特征（11个）
1. **TotalSF** - 总面积（TotalBsmtSF + 1stFlrSF + 2ndFlrSF）
2. **TotalSF_GrLiv** - 总面积+地上居住面积
3. **HouseAge** - 房龄（YrSold - YearBuilt）
4. **RemodAge** - 距上次装修年数（YrSold - YearRemodAdd）
5. **TotalBath** - 总卫生间数（FullBath + BsmtFullBath + 0.5*HalfBath + 0.5*BsmtHalfBath）
6. **TotalPorchSF** - 总室外面积（OpenPorchSF + EnclosedPorch + 3SsnPorch + ScreenPorch）
7. **HasBsmt** - 是否有地下室（二值）
8. **HasGarage** - 是否有车库（二值）
9. **Has2ndFloor** - 是否有二层（二值）
10. **QualityArea** - 质量×面积交互（OverallQual * GrLivArea）
11. **TotalRooms** - 总房间数

#### 偏态特征处理
- 自动识别 |skew| > 0.75 的数值特征
- 对符合条件的特征应用 log1p 变换
- 可视化Top 10偏态特征的变换前分布

#### 异常值处理
- 移除 GrLivArea > 4000 且 SalePrice < 300000 的样本
- 可视化识别和标注异常样本ID

### 3. 模型训练（保持原有架构）

#### 三个基础模型
- **Ridge回归** (alpha=5.0) - 线性基准
- **RandomForest** (800棵树, max_depth=12) - 非线性集成
- **XGBoost** (eta=0.03, max_depth=4) - 梯度提升

#### 新增可视化
- 模型CV得分对比柱状图
- Random Forest特征重要性（Top 30）
- XGBoost特征重要性（Top 30）
- OOF预测 vs 真实值散点图
- 残差分布图

#### 加权集成
- 网格搜索最优权重组合
- 最小化OOF RMSE（log空间）

## 使用方法

### 在Kaggle上运行

1. 上传 `House_Prices_Improved.ipynb` 到Kaggle
2. 添加House Prices数据集：
   - Competition: house-prices-advanced-regression-techniques
3. 运行所有cells

### 本地运行

1. 准备数据文件：
   - train.csv
   - test.csv
   - sample_submission.csv

2. 修改数据路径（如果需要）：
   ```python
   train_candidates = [
       "train.csv",
       "../input/train.csv",
       # 添加您的本地路径
   ]
   ```

3. 安装依赖：
   ```bash
   pip install numpy pandas matplotlib seaborn scikit-learn xgboost scipy
   ```

4. 运行notebook

## Notebook结构（92个cell）

```
1. 标题和介绍
2. 导入库和工具函数
3. 创建eda_visuals.py模块
4. 数据加载
5. 数据探索与可视化
   - 3.3 目标变量深度分析
   - 3.4 数值特征与目标关系
   - 3.5 类别特征分析
   - 3.6 特征交互可视化
   - 3.7 异常值识别
6. 特征工程
   - 4.1 异常值处理
   - 4.2 缺失值处理（改进）
   - 4.3 新特征构造
   - 4.4 偏态特征变换
   - 4.5 类别编码（OneHot）
7. 模型训练与评估
   - 5.1 数据准备
   - 5.2 Ridge训练
   - 5.3 RandomForest训练
   - 5.4 XGBoost训练
   - 5.5 特征重要性分析
   - 5.6 模型集成
8. 预测与提交
9. 总结与改进方向
```

## 预期性能

通过改进的特征工程和模型集成，预期：
- **CV RMSE (log)**: < 0.12
- **更稳定的预测**：减少异常值影响
- **更好的泛化**：10折交叉验证

## 未来改进方向

1. **特征工程**
   - 多项式特征交互
   - 目标编码（Target Encoding）
   - 地理位置特征

2. **模型优化**
   - Optuna超参数优化
   - LightGBM、CatBoost
   - Stacking集成

3. **验证策略**
   - 时间序列交叉验证
   - 分层抽样

## 常见问题

**Q: 为什么有些可视化显示不正常？**
A: 确保安装了所有依赖库，特别是matplotlib和seaborn的最新版本。

**Q: 缺少中文字体怎么办？**
A: 修改eda_visuals.py中的font参数，或注释掉set_plotting调用。

**Q: 如何调整模型超参数？**
A: 在模型定义部分修改参数，例如：
```python
rf_model = RandomForestRegressor(
    n_estimators=1000,  # 增加树的数量
    max_depth=15,       # 增加深度
    ...
)
```

**Q: 可以添加更多特征吗？**
A: 当然！在add_features函数中添加您的特征工程逻辑。

## 联系方式

如有问题或建议，请通过GitHub Issues联系。

---

**Enjoy exploring and improving!** 🚀
